import { HiddenDirectiveDirective } from './hidden-directive.directive';

describe('HiddenDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new HiddenDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
